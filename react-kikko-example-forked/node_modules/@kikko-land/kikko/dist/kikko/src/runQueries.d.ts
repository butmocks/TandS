import { ISqlAdapter } from "@kikko-land/sql";
import { IDbState } from "./types";
export declare const runQueries: <D extends Record<string, unknown>>(state: IDbState, queries: ISqlAdapter[]) => Promise<D[][]>;
export declare const runQuery: <D extends Record<string, unknown>>(state: IDbState, query: ISqlAdapter) => Promise<D[]>;

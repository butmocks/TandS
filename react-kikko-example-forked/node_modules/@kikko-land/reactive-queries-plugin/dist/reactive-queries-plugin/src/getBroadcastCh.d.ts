import { Observable } from "rxjs";
export declare type IMessage = {
    changesInTables: string[];
};
export declare type IListener = (msg: IMessage) => void;
export interface INotifyChannel {
    postMessage(msg: IMessage): Promise<void>;
    addEventListener(cb: IListener): void;
    removeEventListener(cb: IListener): void;
    close(): Promise<void>;
}
export declare const getBroadcastCh: (name: string, webMultiTabSupport: boolean, stop$: Observable<void>) => Observable<INotifyChannel>;

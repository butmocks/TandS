import { IDbState } from "@kikko-land/kikko";
import { ISqlAdapter } from "@kikko-land/sql";
import { Observable } from "rxjs";
export declare const listenQueries: <D extends Record<string, unknown>>(db: IDbState, queries: ISqlAdapter[]) => Observable<D[][]>;

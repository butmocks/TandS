import { ICommand } from "./commands";
import { IBackendState } from "./types";
export declare const runWorkerCommand: (backendState: IBackendState, command: ICommand) => Promise<Record<string, string | number | Uint8Array | null>[][]>;

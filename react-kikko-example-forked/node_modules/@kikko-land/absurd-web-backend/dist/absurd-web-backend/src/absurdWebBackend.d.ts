import { IDbBackend } from "@kikko-land/kikko";
export declare const absurdWebBackend: ({ wasmUrl, queryTimeout, pageSize, cacheSize, }: {
    wasmUrl: string | (() => Promise<string>);
    queryTimeout?: number | undefined;
    pageSize?: number | undefined;
    cacheSize?: number | undefined;
}) => IDbBackend;

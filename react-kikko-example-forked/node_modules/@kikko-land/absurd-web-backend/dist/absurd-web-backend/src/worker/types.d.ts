import { ICommand } from "../commands";
export declare type IResponse = {
    commandId: string;
} & ({
    status: "success";
    result: Record<string, number | string | Uint8Array | null>[][];
} | {
    status: "error";
    message: string;
});
export declare type IOutputWorkerMessage = {
    type: "initialized";
} | {
    type: "response";
    data: IResponse;
};
export declare type IInputWorkerMessage = {
    type: "initialize";
    dbName: string;
    wasmUrl: string;
    pageSize: number;
    cacheSize: number;
} | {
    type: "command";
    data: ICommand;
};

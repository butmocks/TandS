import { IContainsTable, IPrimitiveValue, ISql, ISqlAdapter } from "@kikko-land/sql";
import { IBaseToken, TokenType } from "../types";
export declare const wrapParentheses: <T extends ISqlAdapter | IBaseToken<TokenType> | IPrimitiveValue | IContainsTable>(val: T) => ISql | T;

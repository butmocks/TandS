import { IContainsTable, IPrimitiveValue, ISqlAdapter } from "@kikko-land/sql";
import { IBaseToken, TokenType } from "../types";
export declare type IAlias = IBaseToken<TokenType.Alias> & {
    _left: IBaseToken;
    _right: string;
};
export declare const alias: (left: IBaseToken | ISqlAdapter | IPrimitiveValue | IContainsTable, right: string) => IAlias;

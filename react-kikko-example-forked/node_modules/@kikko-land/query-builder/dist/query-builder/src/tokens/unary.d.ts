import { IPrimitiveValue, ISqlAdapter } from "@kikko-land/sql";
import { IBaseToken, TokenType } from "../types";
export interface IUnaryOperator extends IBaseToken<TokenType.Unary> {
    _operator: "NOT";
    _expr: IBaseToken | ISqlAdapter | IPrimitiveValue;
}
export declare const not: (expr: IBaseToken | ISqlAdapter | IPrimitiveValue) => IUnaryOperator;
